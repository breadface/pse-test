export const headerStyles = {
  bar: {
    height: 50,
    flexDirection: "row",
    alignItems: "center"
  }
};

export const loaderStyles = theme => ({
  progress: {
    margin: theme.spacing.unit * 2
  },
  wrapper: {
    minHeight: "100vh"
  }
});
