import { theme } from "../../config";
export const regionStyles = {
  card: {
    height: 120,
    cursor: "pointer",
    "&:hover": {
      backgroundColor: theme.colors.purple
    }
  },
  title: {
    fontSize: 14
  }
};
